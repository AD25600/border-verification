import { PlaceholderScreen } from "@/components/layout/PlaceholderScreen";

export default function SettingsPage() {
  return (
    <PlaceholderScreen
      title="Settings"
      description="Risk-rule configuration, retention policy settings, and system preferences will appear here in later phases."
    />
  );
}
